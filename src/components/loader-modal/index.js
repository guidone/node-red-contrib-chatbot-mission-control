import React from 'react';
import { Loader } from 'rsuite';

export default () => (
  <div style={{ minHeight: '300px', textAlign: 'center', paddingTop: '80px' }}>
    <Loader size="md" />
  </div>
);